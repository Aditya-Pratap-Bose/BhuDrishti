async function generateExport(format){if(!state.features.length){notify('Run feature extraction or load a feature set first.','error');return}const endpoint=format==='csv'?'/exports/csv':'/exports/geojson';const data=await apiFetch(endpoint,{method:'POST',body:JSON.stringify({features:state.features,metadata:{project_id:state.projectId}})},'v2');if(format==='geojson'){const blob=new Blob([JSON.stringify(data,null,2)],{type:'application/geo+json'});const link=document.createElement('a');link.href=URL.createObjectURL(blob);link.download='bhudrishti-v2-features.geojson';link.click()}notify(`${format.toUpperCase()} export generated.`,'success')}